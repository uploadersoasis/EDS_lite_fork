name: Android CI - Build and Publish APK

on:
  push:
    branches: [ "master" ]
  pull_request:
    branches: [ "master" ]
  workflow_dispatch:

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
    - name: Checkout Repository
      uses: actions/checkout@v4

    - name: Set up JDK 17
      uses: actions/setup-java@v4
      with:
        java-version: '17'
        distribution: 'temurin'
        cache: gradle

    - name: Grant execute permission for gradlew
      run: chmod +x gradlew

    - name: Build Debug APK with Gradle
      # We use 'assembleDebug' to create the unsigned debug APK.
      run: ./gradlew assembleDebug --warning-mode all

    - name: Find Built APK Path
      # FIX: Search recursively for the debug APK file anywhere within the project's build directories.
      id: find_apk
      run: echo "APK_PATH=$(find ${{ github.workspace }} -path "*/build/outputs/apk/debug/*.apk" -print -quit)" >> $GITHUB_OUTPUT
      shell: bash

    - name: Upload Debug APK Artifact
      uses: actions/upload-artifact@v4
      with:
        name: eds-lite-debug-apk
        # This now uses the dynamically found path, which should be correct.
        path: ${{ steps.find_apk.outputs.APK_PATH }}
        retention-days: 5
